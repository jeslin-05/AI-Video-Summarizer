# 🎥 AI Video Summarizer

A full-stack AI application that generates multilingual summaries from YouTube videos.

## 🚀 Features
- **AI Summary:** Extracts audio and summarizes content using BART models.
- **Multilingual:** Translates summaries into Tamil, Telugu, Hindi, Kannada, Sanskrit, etc.
- **Auth System:** Secure Login/Signup connected to XAMPP MySQL.
- **Modern UI:** Glassmorphism design with animated smoke nebula effects.

## 🛠️ Setup
1. **Backend:**
   - Install requirements: `pip install -r requirements.txt`
   - Start server: `uvicorn main:app --reload`
2. **Frontend:**
   - Install dependencies: `npm install`
   - Run app: `npm run dev`
3. **Database:**
   - Import the provided `.sql` file into XAMPP phpMyAdmin.